import { TextWriter } from '@yellicode/core';
export declare class CSharpCommentWriter {
    private writer;
    private maxCommentWidth;
    constructor(writer: TextWriter, maxCommentWidth: number);
    writeDelimitedCommentParagraph(text: string): void;
    writeDelimitedCommentLines(lines: string[]): void;
    writeCommentLines(lines: string[], prefix?: string): void;
}
