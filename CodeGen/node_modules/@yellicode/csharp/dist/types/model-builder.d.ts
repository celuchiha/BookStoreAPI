import * as elements from '@yellicode/elements';
import * as opts from './options';
import { ClassDefinition, InterfaceDefinition } from './model';
export declare class ModelBuilder {
    static buildClassDefinition(type: elements.Type, options?: opts.ClassOptions): ClassDefinition;
    static buildInterfaceDefinition(type: elements.Type, options?: opts.InterfaceOptions): InterfaceDefinition;
    private static buildInherits(type, additional);
    private static buildImplements(type, additional);
    private static buildXmlDocSummary(element);
}
